# Локальный профиль — содержит чувствительные данные.
# TODO: добавить этот файл в .gitignore, чтобы он не попал в репозиторий.

spring:
  ai:
    openai:
      base-url: https://chat.ehd-zr.cbr.ru
      api-key: sk-09fd660cdc8640ac861fe85a16d2d2f1
      chat:
        options:
          model: Qwen/Qwen3-VL-235B-A22B-Instruct-FP8
        completions-path: /openai/chat/completions

app:
  gitlab:
    url: http://localhost
    token: changeme
    webhook-token: changeme
